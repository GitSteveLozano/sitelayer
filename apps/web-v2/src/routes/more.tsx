import { PlaceholderScreen } from '@/components/shell/PlaceholderScreen'

export default function MoreRoute() {
  return (
    <PlaceholderScreen eyebrow="Tab · More" title="More" designId="nav-more">
      Workspace and personal settings live here on both mobile and desktop — no surface drift between devices. Settings,
      pricing book, integrations, profile, privacy, help. Lands incrementally as each phase introduces its config.
    </PlaceholderScreen>
  )
}
