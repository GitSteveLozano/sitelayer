import { PlaceholderScreen } from '@/components/shell/PlaceholderScreen'

export default function ProjectsRoute() {
  return (
    <PlaceholderScreen eyebrow="Tab · Projects" title="Projects" designId="prj-list">
      Phase 2 lands the project list, project detail with sub-tabs (Overview / Takeoff / Schedule / Time), and the new
      estimate flow. Phase 3 wires the takeoff overhaul under the Takeoff sub-tab.
    </PlaceholderScreen>
  )
}
