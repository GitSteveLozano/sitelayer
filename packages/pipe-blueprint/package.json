{
  "name": "@sitelayer/pipe-blueprint",
  "version": "0.0.0",
  "description": "PDF blueprint → TakeoffResult via Claude vision.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "demo": "tsx src/cli.ts",
    "lint": "npm run typecheck"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@sitelayer/capture-schema": "*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.20.3",
    "typescript": "^6.0.3",
    "vitest": "^3.2.4"
  },
  "private": true
}
